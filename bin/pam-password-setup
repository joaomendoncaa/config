#!/usr/bin/env bash
set -euo pipefail

if [[ $EUID -ne 0 ]]; then
    echo "Must be run as root (sudo)." >&2
    exit 1
fi

install -m 644 /dev/stdin /etc/pam.d/lock-password <<'EOF'
auth include system-auth
account include system-auth
EOF

echo "Created /etc/pam.d/lock-password"
