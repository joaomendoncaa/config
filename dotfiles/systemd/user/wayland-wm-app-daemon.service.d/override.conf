# Fix: the app daemon launches apps via `systemd-run --scope --same-dir`,
# so spawned apps inherit THIS daemon's working directory. systemd services
# default to cwd=/, which breaks Chromium's singleton handoff (browser opens
# links in a new window instead of a tab). Use the user's home as cwd.
[Service]
WorkingDirectory=%h
